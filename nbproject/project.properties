annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processor.options=-Aeclipselink.canonicalmodel.use_static_factory=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output
application.title=Sistema-Centro-Educativo
application.vendor=Usuario
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# Files in build.classes.dir which should be excluded from distribution jar
dist.archive.excludes=
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Sistema-Centro-Educativo.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.gson-2.2.4.jar=libs\\gson-2.2.4.jar
includes=**
jar.compress=false
javac.classpath=\
    ${libs.eclipselink.classpath}:\
    ${reference.carrera.jar}:\
    ${reference.motor.jar}:\
    ${reference.persona.jar}:\
    ${file.reference.gson-2.2.4.jar}:\
    ${libs.MySQLDriver.classpath}:\
    ${reference.asignacion_carrera.jar}:\
    ${reference.asignacion_catedratico.jar}:\
    ${reference.asignacion_curso.jar}:\
    ${reference.asignacion_estudiante.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.external.vm=true
javac.processorpath=\
    ${javac.classpath}:\
    ${libs.eclipselinkmodelgen.classpath}
javac.source=1.8
javac.target=1.8
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javac.test.processorpath=\
    ${javac.test.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=Principal.Principal2
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
project.asignacion_carrera=MODULOS/asignacion.carrera
project.asignacion_catedratico=MODULOS/asignacion.catedratico
project.asignacion_curso=MODULOS/asignacion.curso
project.asignacion_estudiante=MODULOS/asignacion.estudiante
project.carrera=MODULOS/carrera
project.motor=MODULOS/motor
project.persona=MODULOS/persona
reference.asignacion_carrera.jar=${project.asignacion_carrera}/dist/asignacion.carrera.jar
reference.asignacion_catedratico.jar=${project.asignacion_catedratico}/dist/asignacion.catedratico.jar
reference.asignacion_curso.jar=${project.asignacion_curso}/dist/asignacion.curso.jar
reference.asignacion_estudiante.jar=${project.asignacion_estudiante}/dist/asignacion.estudiante.jar
reference.carrera.jar=${project.carrera}/dist/carrera.jar
reference.motor.jar=${project.motor}/dist/motor.jar
reference.persona.jar=${project.persona}/dist/persona.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project.
# You may also define separate properties like run-sys-prop.name=value instead of -Dname=value.
# To set system properties for unit tests define test-sys-prop.name=value:
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
